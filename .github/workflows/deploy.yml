# GENERATED by UDAP Build Project from .udap/pipeline.yaml — do not
# edit by hand; change the spec and re-render.
name: deploy
'on':
  workflow_dispatch: {}
jobs:
  lint:
    runs-on: ubuntu-latest
    env:
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: temurin
    - name: Bootstrap Gradle wrapper jar from distribution zip
      run: "curl -fsSL -o /tmp/gradle-8.6-bin.zip \\\n  https://services.gradle.org/distributions/gradle-8.6-bin.zip\n\
        unzip -j /tmp/gradle-8.6-bin.zip \\\n  \"gradle-8.6/lib/gradle-wrapper-*.jar\"\
        \ \\\n  -d /tmp/gradle-wrapper-extracted/\ncp /tmp/gradle-wrapper-extracted/gradle-wrapper-*.jar\
        \ \\\n  gradle/wrapper/gradle-wrapper.jar\nJAR_SIZE=$(stat -c%s gradle/wrapper/gradle-wrapper.jar)\n\
        echo \"Gradle wrapper jar bootstrapped: ${JAR_SIZE} bytes\"\nif [ \"$JAR_SIZE\"\
        \ -lt 10000 ]; then\n  echo \"ERROR: wrapper jar too small\"\n  exit 1\nfi\n"
    - name: Accept Android SDK licenses and install platform
      run: 'SDKMANAGER="/usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager"

        yes | "$SDKMANAGER" --licenses

        "$SDKMANAGER" "platforms;android-34" "build-tools;34.0.0"

        '
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        validate-wrappers: false
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Compile Kotlin sources (lint check)
      run: ./gradlew compileDebugKotlin
    - name: Run Android Lint (non-blocking)
      run: ./gradlew lintDebug || echo "Lint completed (warnings may exist)"
  test:
    runs-on: ubuntu-latest
    needs:
    - lint
    env:
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: temurin
    - name: Bootstrap Gradle wrapper jar from distribution zip
      run: "curl -fsSL -o /tmp/gradle-8.6-bin.zip \\\n  https://services.gradle.org/distributions/gradle-8.6-bin.zip\n\
        unzip -j /tmp/gradle-8.6-bin.zip \\\n  \"gradle-8.6/lib/gradle-wrapper-*.jar\"\
        \ \\\n  -d /tmp/gradle-wrapper-extracted/\ncp /tmp/gradle-wrapper-extracted/gradle-wrapper-*.jar\
        \ \\\n  gradle/wrapper/gradle-wrapper.jar\nJAR_SIZE=$(stat -c%s gradle/wrapper/gradle-wrapper.jar)\n\
        echo \"Gradle wrapper jar bootstrapped: ${JAR_SIZE} bytes\"\nif [ \"$JAR_SIZE\"\
        \ -lt 10000 ]; then\n  echo \"ERROR: wrapper jar too small\"\n  exit 1\nfi\n"
    - name: Accept Android SDK licenses and install platform
      run: 'SDKMANAGER="/usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager"

        yes | "$SDKMANAGER" --licenses

        "$SDKMANAGER" "platforms;android-34" "build-tools;34.0.0"

        '
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        validate-wrappers: false
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Run Unit Tests
      run: ./gradlew test
    - name: Upload Test Results
      uses: actions/upload-artifact@v4
      with:
        name: test-results
        path: app/build/reports/tests/
        if-no-files-found: warn
  provision:
    runs-on: ubuntu-latest
    needs:
    - test
    timeout-minutes: 5
    steps:
    - uses: actions/checkout@v4
    - name: No-op provision (CI-only project)
      run: echo "No cloud infrastructure required for this project"
  configure:
    runs-on: ubuntu-latest
    needs:
    - provision
    env:
      ANDROID_HOME: /usr/local/lib/android/sdk
      ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
    timeout-minutes: 25
    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: temurin
    - name: Bootstrap Gradle wrapper jar from distribution zip
      run: "curl -fsSL -o /tmp/gradle-8.6-bin.zip \\\n  https://services.gradle.org/distributions/gradle-8.6-bin.zip\n\
        unzip -j /tmp/gradle-8.6-bin.zip \\\n  \"gradle-8.6/lib/gradle-wrapper-*.jar\"\
        \ \\\n  -d /tmp/gradle-wrapper-extracted/\ncp /tmp/gradle-wrapper-extracted/gradle-wrapper-*.jar\
        \ \\\n  gradle/wrapper/gradle-wrapper.jar\nJAR_SIZE=$(stat -c%s gradle/wrapper/gradle-wrapper.jar)\n\
        echo \"Gradle wrapper jar bootstrapped: ${JAR_SIZE} bytes\"\nif [ \"$JAR_SIZE\"\
        \ -lt 10000 ]; then\n  echo \"ERROR: wrapper jar too small\"\n  exit 1\nfi\n"
    - name: Accept Android SDK licenses and install platform
      run: 'SDKMANAGER="/usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager"

        yes | "$SDKMANAGER" --licenses

        "$SDKMANAGER" "platforms;android-34" "build-tools;34.0.0"

        '
    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        validate-wrappers: false
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build Debug APK
      run: ./gradlew assembleDebug
    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: kotlin-todo-app-debug
        path: app/build/outputs/apk/debug/app-debug.apk
        retention-days: 30
  verify:
    runs-on: ubuntu-latest
    needs:
    - configure
    timeout-minutes: 5
    steps:
    - uses: actions/checkout@v4
    - name: Download APK artifact
      uses: actions/download-artifact@v4
      with:
        name: kotlin-todo-app-debug
        path: apk-download
    - name: Verify APK exists
      run: "APK_PATH=\"apk-download/app-debug.apk\"\nif [ ! -f \"$APK_PATH\" ]; then\n\
        \  echo \"ERROR: Debug APK not found at $APK_PATH\"\n  exit 1\nfi\necho \"\
        APK size: $(du -sh $APK_PATH | cut -f1)\"\necho \"Build artifact verified\
        \ successfully\"\nls -lh apk-download/\n"
